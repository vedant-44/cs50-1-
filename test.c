#include<stdio.h>
#include<cs50.h>
#include<stdlib.h>
int main(void)
{

    int i=0;
    char* name=get_string("name:");
    if(true)
    {

        FILE* img=fopen
        fprintf(img,"%s",name);
        fclose(img);

    }
   FILE* img=fopen("photo.csv","a");
    fprintf(img,"\nabhinav");

}