#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    char* photo=malloc(100);
    FILE* img=NULL;
    for(int i=0;i<2;i++)
    {
        if(i==0)
        {
            sprintf(photo,"%03i.csv",0);
            img=fopen
        }
    }

}